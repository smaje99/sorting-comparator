# IDE metadata
/.idea/
/*.iml

# Build outputs
/out/
/target/

# Libraries
/.mvn/
/.mvnw
/.mvnw.cmd
/lib/

# Legacy reference source outside the Maven runtime
/src/def/*

# Keep vendor/maven-repository tracked.
# The Maven build resolves the vendored Constrains dependency from there.
